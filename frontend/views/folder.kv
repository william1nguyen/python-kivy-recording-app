<FolderWindow>:
    name: "folder_screen"
    BoxLayout:
        orientation: "vertical"

        RoundedButton:
            id: back_button
            text: "Log Out"
            size_hint: None, None
            on_release: 
                app.root.current = "option_screen"
                root.manager.transition.direction = "left"
